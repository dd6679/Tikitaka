using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NK_FogItem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // 안개 생성
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
